/**
 * Re-export for Turbopack path alias `@/*` → `./src/*` (also mapped via root `lib/`).
 * Canonical implementation: lib/ratings/company-rating.ts
 */
export * from '../../../lib/ratings/company-rating';
